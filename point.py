class Point:

    def __init__(self,X,Y):
        self.X = X
        self.Y = Y

    def slope (self,x,y):
        thing = self.X-self.Y/x-y
        if thing + -thing == 0:
            z = 'positive'
        else:
            z = 'negative'
        return z



    def quadrant (self):
        if self.X + -self.X and self.Y + -self.Y:
            quad = '1'

        elif 

    def
